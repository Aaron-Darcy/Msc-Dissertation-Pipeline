google-cloud-bigquery>=3.25.0
db-dtypes>=1.3.0
pandas>=2.2.0
pyarrow>=17.0.0
sdv==1.17.0          # the version the reported results were produced with
scikit-learn>=1.5.0  # resolved version not recorded at run time
jupyter>=1.1.0
ipykernel>=6.29.0
matplotlib>=3.9.0
seaborn>=0.13.0

# Notebook 06 only. Installed inside the notebook because it pins numpy and torch
# below the versions Colab ships, and the matched set must resolve in one pass.
# synthcity==0.2.12
# torch==2.2.2
# torchvision==0.17.2
# numpy==1.26.4
